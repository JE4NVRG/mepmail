version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 10
    ignore:
      # Types must not run ahead of the node:24 runtime in the Dockerfile; bump
      # them by hand with the image.
      - dependency-name: "@types/node"
        update-types: ["version-update:semver-major"]
      # Tests must run the same Postgres major as production (postgres:17-alpine);
      # pglite 0.4+ embeds Postgres 18, which reports a different SQLSTATE for
      # RESTRICT violations and inherits the host time zone. Lift with the DB.
      - dependency-name: "@electric-sql/pglite"
        versions: [">=0.4.0"]
      # 1.7.3+ never writes account.issuer and refuses our NOT NULL column at
      # init; lift once the column is nullable (better-auth 1.7 upgrade guide).
      - dependency-name: "better-auth"
        versions: [">=1.7.3"]
      - dependency-name: "@better-auth/oauth-provider"
        versions: [">=1.7.3"]
    groups:
      production-dependencies:
        dependency-type: production
      development-dependencies:
        dependency-type: development
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
  - package-ecosystem: docker
    directory: /
    schedule:
      interval: weekly
  - package-ecosystem: docker
    directory: /scripts/backup
    schedule:
      interval: weekly
